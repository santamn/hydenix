# Agent instructions

## Writing PR descriptions

Follow the structure in `.github/pull_request_template.md`. 

Commit messages follow Conventional Commits and are linted by
`commitlint` (`.commitlintrc.json`) against types: `build`, `chore`, `ci`,
`docs`, `feat`, `fix`, `perf`, `refactor`, `revert`, `style`, `test`.
